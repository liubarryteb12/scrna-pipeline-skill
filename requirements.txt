# ============================================================================
# requirements.txt — 单细胞流水线依赖
# ============================================================================
# 分三档，**不合并成一个列表**，因为它们的失败后果不同：
#
#   [核心]   缺任何一个，流水线跑不了 —— CI 必须装成功
#   [可选]   缺了会降级并记录原因（如 HVG 口味从 seurat_v3 退到 seurat）
#   [外部]   数据库/方法包，体量大或编译敏感，装了才走那条路
#
# **不要用 `>=` 不带上限。** scanpy 的绘图 API 在小版本间改过
# （`sc.pl.highly_variable_genes` 的 `return_fig` 在 1.12 被移除），
# 不钉版本会让 CI 在某天突然红，而原因是上游改了 API 不是我们的 bug。
# ============================================================================

# ---- 核心 -------------------------------------------------------------------
numpy>=1.26,<3
scipy>=1.11
pandas>=2.1
matplotlib>=3.8
PyYAML>=6.0
anndata>=0.10,<0.14
scanpy>=1.10,<1.13
scikit-learn>=1.3
statsmodels>=0.14
h5py>=3.10

# 图聚类（leiden 需要 igraph + leidenalg）
igraph>=0.11
leidenalg>=0.10
umap-learn>=0.5

# ---- 可选（缺了降级并记录）--------------------------------------------------
# scrublet 依赖 scikit-image 做模拟双细胞的邻居图
scikit-image>=0.22
# seurat_v3 口味 HVG 依赖 scikit-misc 的 loess 拟合
scikit-misc>=0.3

# ---- 方法包（装了才走那条路）------------------------------------------------
# 拟bulk 差异表达
pydeseq2>=0.4
# 批次整合（多供本数据需要）
harmonypy>=0.0.10

# ---- 轨迹推断（step 05 的交叉验证方法）--------------------------------------
# **至少两种方法才叫交叉验证。** 实测（PBMC3k）三种方法的原始拟时序两两
# Spearman 相关从 -0.47 到 +0.25 —— 符号都不一样。只有一种方法时，
# 报出来的"轨迹"是某个算法的一次输出，不是数据里的结构。
#
# palantir: 扩散图 + Markov 链，给出拟时序与命运概率。
#   依赖全都有 wheel（scanpy/mellon/anndata/ml_dtypes/igraph/...），
#   无编译、无 GPU。会连带装 jax —— 大但稳。
palantir>=1.4,<2
# scFates: Slingshot 的 Python 移植（主曲线树）。
#   注意 `tl.pseudotime()` 之前**必须**先 `tl.cleanup()`，否则必崩在
#   `pd.Series(uns['graph']['milestones']) == t][0]` 的 IndexError。
#   另：`tl.test_association` / `tl.test_fork` 需要 rpy2 + R + mgcv，
#   与"云端不装 R"的设计冲突，所以**没有用**这两个函数 ——
#   沿轨迹的基因分析是本仓库自己实现的（见 05_trajectory.py）。
scFates>=1.2,<2

