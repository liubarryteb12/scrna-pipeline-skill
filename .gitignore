# 数据与产物：**不入库**
# 单细胞数据动辄几百 MB 到几十 GB，入库会让 clone 变得不可用。
# CI 每次都重新下载（内置数据集只有 7 MB）。
# **用前导 **/ 递归匹配** —— 只写 data/ 不覆盖 scripts/lib/data/，
# 实测漏了一个 24 MB 的缓存 h5ad（用户 2026-09-25 打包时发现）。
**/data/
**/results/

# 缓存
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/

# 环境
.venv/
venv/
env/
*.egg-info/

# 编辑器
.vscode/
.idea/
*.swp
.DS_Store

# 临时
*.part
*.tmp
